@* <section class="folder-studio" id="folder-studio">
    <h2>@(isEditingFolder ? "Edit Folder" : "New Folder")</h2>

    <div class="studio-field">
        <label for="folder-name">Folder name</label>
        <InputText id="folder-name" class="studio-input" @bind-Value="folderModel.Name" />
    </div>

    <div class="studio-field">
        <label for="folder-description">Description</label>
        <InputTextArea id="folder-description" class="studio-input studio-textarea" rows="4" @bind-Value="folderModel.Description" />
    </div>

    <label class="studio-toggle">
        <InputCheckbox @bind-Value="folderModel.IsQuickAccess" />
        <span>Show in quick access</span>
    </label>

    <div class="studio-actions">
        <button type="button" class="studio-primary" @onclick="SaveFolderAsync">
            @(isEditingFolder ? "Update folder" : "Create folder")
        </button>
        <button type="button" class="studio-secondary" @onclick="ResetFolderForm">Clear</button>
    </div>
</section>

@code{
    private Folder folderModel = new Folder();

    private bool isEditingFolder = false;
} *@    